
# Please enter the commit message for your changes. Lines starting
# with '#' will be ignored, and an empty message aborts the commit.
# On branch master
# Changes to be committed:
#   (use "git reset HEAD <file>..." to unstage)
#
#	modified:   public/javascripts/controllers/contacts.js
#
diff --git a/public/javascripts/controllers/contacts.js b/public/javascripts/controllers/contacts.js
index 7890132..5279986 100644
--- a/public/javascripts/controllers/contacts.js
+++ b/public/javascripts/controllers/contacts.js
@@ -344,10 +344,14 @@ $(function() {
           groupId = getActiveGroupId();
       // TODO check the user quota if user is allowed to add more contacts to the list
       var totalContacts = $('#contacts-list').find('.active li').size();
-      if(totalContacts >= contactsQuota){
+      if(totalContacts >= (contactsQuota -1)){
         $('#contacts-add, #restriction').addClass('disabled');
-        $.helpers.notice('Please upgrade to premium to add more contacts to the list');
-        return false;
+        
+        if (totalContacts >= contactsQuota) {
+          $.helpers.notice('Please upgrade to premium to add more contacts to the list');
+          
+          return false;
+        }
       }
       
       // modify it's id
@@ -392,6 +396,11 @@ $(function() {
       $item.remove();
       // update the count
       $('.active abbr').increment(-1);
+      var totalContacts = $('#contacts-list').find('.active li').size();
+
+      if(totalContacts < contactsQuota){
+        $('#contacts-add, #restriction').removeClass('disabled');
+      }
       // make the backend call
       $.scAjax({
         url: groupMemberUri($item),
